services:
  timepulse:
    build: .
    container_name: timepulse
    restart: unless-stopped
    ports:
      - "3456:3456"
    volumes:
      - timepulse-data:/data
    env_file:
      - .env
    environment:
      DB_PATH: /data/timepulse.db

volumes:
  timepulse-data:
